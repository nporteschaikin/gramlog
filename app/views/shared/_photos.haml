%section.photos{"class" => cls}
	%nav.sticky.left
		%ul
			%li.red
				%span
					= title
	- feed.each do |gram|
		%article
			= link_to "Instagram", "#"
			%address
				= instagram_user_link(gram)
			= instagram_image(gram)